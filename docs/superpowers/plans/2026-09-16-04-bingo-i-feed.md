# Bingo i feed — plan implementacji

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dać aplikacji pierwsze prawdziwe źródło punktów — planszę bingo ze zdjęciami jako dowodami, akceptacją przez wielu adminów i bonusami za linie — oraz globalny feed z lajkami i komentarzami.

**Architecture:** Plansza to dwadzieścia pięć zadań wspólnych dla wszystkich drużyn; stan jest per drużyna i trzyma go `bingo_submissions` z unikalnością `(team_id, task_id)` dla nieodrzuconych. Akceptacja, punkty, wykrycie linii i bonusy dzieją się w jednej funkcji `SECURITY DEFINER`, w jednej transakcji. Bonusy są idempotentne przez dane: wpis w księdze nosi `ref_type` i `ref_id` identyfikujące linię, a funkcja sprawdza istnienie przed dopisaniem. Feed czyta te same zgłoszenia, więc nie ma osobnego modelu treści.

**Tech Stack:** Next.js 16, TypeScript, Tailwind v4, Supabase (Postgres + Storage + Realtime), Vitest.

**Spec:** `docs/superpowers/specs/2026-09-16-bingo-i-feed-design.md`

**Spec główny:** `docs/superpowers/specs/2026-09-14-sekta-wyjazdowa-design.md` — §9 krok 4, decyzje D1 i D2

---

## Co już sprawdziłem na prawdziwej bazie

Żeby plan nie stał na założeniach, przetestowałem dwie rzeczy zapytaniami przeciwko
projektowi testowemu:

**Wykrywanie linii przez zawieranie tablic.** `array[0,1,2,3,4] <@ array[0,1,2,3,4,7,9]`
zwraca prawdę, `array[0,1,2,3,4] <@ array[0,1,2,7]` fałsz. Zapytanie z dwunastoma
liniami w `values` i `<@` przeciwko tablicy zapalonych pól zwraca dokładnie te
kompletne — sprawdzone na zestawie, gdzie pełne były wiersz i przekątna, a kolumna nie.

**Odczyt liczby z `app_settings`.** Kolumna jest typu `jsonb`, więc `value::int` nie
zadziała. `(value #>> '{}')::int` zwraca poprawnie `300` dla zasianego
`casino_daily_stake_cap`.

---

## Jak czytać ten plan

Taski 1, 2 i 4 zawierają kompletny kod — bo SQL bonusów i pomocnik zapalający
pola to miejsca, w których pomyłka jest cicha i kosztowna.

Taski 3, 5, 6, 7 i 8 opisują **intencję, ograniczenia i plik-wzorzec**, zamiast
przepisywać kod, który w tym repozytorium już istnieje w trzech wariantach.
Testy RLS mają wzorzec w `tests/db/registrations.test.ts`, ekran z uploadem
w `src/app/rejestracja/FormularzRejestracji.tsx`, a kolejka admina
w `src/app/(apka)/admin/rejestracje/`. To świadome odstępstwo od zasady
„pełny kod w każdym kroku": powielanie trzysta linii, które implementer i tak
musi przeczytać, żeby zachować spójność, kosztuje więcej, niż daje.

**Każdy taki task wskazuje konkretny plik do naśladowania.** Jeśli w trakcie
okaże się, że wzorzec nie pasuje, to jest sygnał do zatrzymania się i zapytania,
a nie do improwizacji.

## Pułapki, na których ten projekt już się przejechał

Wszystkie kosztowały osobny cykl poprawek. Nie powtarzaj ich.

**Znaczniki migracji.** `npx supabase migration new` bierze bieżący czas UTC.
Wcześniejsze migracje mają znaczniki wpisane z ręki. **Sprawdź wygenerowaną nazwę,
zanim wpiszesz treść** — musi być późniejsza niż `20260916122716`.

**Nazwy promieni:** `rounded-sm/md/lg`, nigdy `rounded-s/m/l`. W Tailwindzie v4 `s`
znaczy „border-start", a `l` znaczy „border-left", i wbudowana reguła wygrywa
w kaskadzie.

**Warstwy CSS.** `.szklo` siedzi w `@layer components`, żeby utility Tailwinda
(`hover:`, `focus-visible:`, `shadow-*`) miały nad nią pierwszeństwo. Nie przenoś.

**Czerwień jako tekst:** `text-krew-jasna`. `text-krew` zostaje do obramowań i teł.

**Subskrypcja realtime** wymaga `config.postgres_changes_options.wait = true` —
bez tego `SUBSCRIBED` przychodzi, zanim serwer uruchomi subskrypcję, i zdarzenia
z tego okna giną bez śladu.

**Limit logowań w testach:** trzydzieści na pięć minut w projekcie testowym.
`AuthApiError: Request rate limit reached` to limit usługi, nie regres — odczekaj
pięć minut.

**Ścieżki z nawiasami** (`src/app/(apka)`) zawsze w cudzysłowach w basha.

---

## Struktura plików po tym planie

```
supabase/migrations/
  <ts>_bingo_i_feed.sql          tabele, RLS, bucket, zasiew 25 zadań, ustawienia
  <ts>_akceptacja_bingo.sql      funkcja review_bingo z bonusami
src/
  app/(apka)/
    bingo/
      page.tsx                   plansza drużyny (serwer)
      Plansza.tsx                klient: pola, wybór, upload
    feed/
      page.tsx                   strumień zaakceptowanych (serwer)
      Wpis.tsx                   klient: lajk i komentarze
    admin/
      bingo/page.tsx             kolejka zgłoszeń bingo
      bingo/DecyzjaBingo.tsx     klient: przyjmij / odrzuć
  types/db.ts                    + BingoTask, BingoSubmission, Komentarz
tests/db/
  bingo-rls.test.ts              polityki tabel i bucketu
  bingo-akceptacja.test.ts       funkcja, punkty, bonusy, idempotentność
  feed-spoleczne.test.ts         lajki i komentarze
```

---

## Task 1: Migracja — tabele, RLS, bucket, zasiew

**Files:**
- Create: `supabase/migrations/<timestamp>_bingo_i_feed.sql`

- [ ] **Step 1: Utwórz plik migracji**

```bash
cd "/d/Projects/jwk26"
npx supabase migration new bingo_i_feed
```

**Sprawdź wygenerowaną nazwę.** Musi być późniejsza niż `20260916122716`. Jeśli nie
jest, przemianuj na `20260917100000_bingo_i_feed.sql`.

- [ ] **Step 2: Wpisz treść**

```sql
-- ============================================================
-- Sekta Wyjazdowa — bingo i feed
-- ============================================================

-- ---------- Zadania ----------
-- Wspólne dla wszystkich drużyn. Stan planszy jest per drużyna i trzyma go
-- bingo_submissions — tutaj siedzi wyłącznie treść zadania.
create table bingo_tasks (
  id          uuid primary key default gen_random_uuid(),
  position    integer not null unique check (position between 0 and 24),
  title       text not null,
  description text not null,
  points      integer not null default 20 check (points between 1 and 1000),
  active      boolean not null default true,
  created_at  timestamptz not null default now()
);

-- ---------- Zgłoszenia ----------
create table bingo_submissions (
  id          uuid primary key default gen_random_uuid(),
  team_id     uuid not null references teams(id) on delete cascade,
  user_id     uuid not null references profiles(id) on delete cascade,
  task_id     uuid not null references bingo_tasks(id) on delete cascade,
  photo_path  text not null,
  caption     text check (caption is null or length(caption) <= 300),
  status      user_status not null default 'pending',
  reviewed_by uuid references profiles(id) on delete set null,
  reviewed_at timestamptz,
  review_note text,
  created_at  timestamptz not null default now()
);

-- Jedno nieodrzucone zgłoszenie na pole i drużynę. Odrzucenie zwalnia pole samo,
-- bez żadnej dodatkowej akcji — warunek obejmuje wyłącznie nieodrzucone.
create unique index bingo_jedno_na_pole
  on bingo_submissions (team_id, task_id)
  where status <> 'rejected';

create index bingo_submissions_feed_idx
  on bingo_submissions (status, created_at desc);

-- ---------- Warstwa społeczna ----------
create table feed_likes (
  submission_id uuid not null references bingo_submissions(id) on delete cascade,
  user_id       uuid not null references profiles(id) on delete cascade,
  created_at    timestamptz not null default now(),
  primary key (submission_id, user_id)
);

create table feed_comments (
  id            uuid primary key default gen_random_uuid(),
  submission_id uuid not null references bingo_submissions(id) on delete cascade,
  user_id       uuid not null references profiles(id) on delete cascade,
  body          text not null check (length(trim(body)) between 1 and 500),
  created_at    timestamptz not null default now()
);

create index feed_comments_submission_idx
  on feed_comments (submission_id, created_at);

-- ============================================================
-- RLS
-- ============================================================

alter table bingo_tasks        enable row level security;
alter table bingo_submissions  enable row level security;
alter table feed_likes         enable row level security;
alter table feed_comments      enable row level security;

-- Zadania: czyta każdy zaakceptowany, pisze admin.
create policy bingo_tasks_read on bingo_tasks
  for select to authenticated
  using (public.is_approved() or public.is_admin());

create policy bingo_tasks_admin_write on bingo_tasks
  for all to authenticated
  using (public.is_admin()) with check (public.is_admin());

-- Zgłoszenia: czyta każdy zaakceptowany, bo feed jest globalny.
create policy bingo_submissions_read on bingo_submissions
  for select to authenticated
  using (public.is_approved() or public.is_admin());

-- Wstawia wyłącznie we własnym imieniu, dla własnej drużyny, ze statusem
-- oczekującym i z plikiem we własnym folderze. Ostatni warunek jest tym samym
-- zabezpieczeniem, które przy dowodach przelewu okazało się konieczne: bez niego
-- da się podpiąć cudzy plik pod własne zgłoszenie.
create policy bingo_submissions_insert_own on bingo_submissions
  for insert to authenticated
  with check (
    user_id = (select auth.uid())
    and team_id = (select team_id from public.profiles where id = (select auth.uid()))
    and status = 'pending'
    and reviewed_by is null
    and reviewed_at is null
    and photo_path like ((select auth.uid())::text || '/%')
    and photo_path !~ '\.\.'
  );

-- Autor wycofuje własne, póki czeka. Odblokowuje to pole reszcie drużyny,
-- zamiast trzymać ją zakładnikiem jednego kiepskiego wrzutu do czasu, aż admin
-- dojdzie do kolejki.
create policy bingo_submissions_delete_own on bingo_submissions
  for delete to authenticated
  using (user_id = (select auth.uid()) and status = 'pending');

-- Brak polityki UPDATE: status zmienia wyłącznie review_bingo.

-- Lajki: czyta każdy zaakceptowany, stawia i cofa wyłącznie swoje.
create policy feed_likes_read on feed_likes
  for select to authenticated
  using (public.is_approved() or public.is_admin());

create policy feed_likes_insert_own on feed_likes
  for insert to authenticated
  with check (user_id = (select auth.uid()) and public.is_approved());

create policy feed_likes_delete_own on feed_likes
  for delete to authenticated
  using (user_id = (select auth.uid()));

-- Komentarze: czyta każdy zaakceptowany, pisze swoje, kasuje autor albo admin.
create policy feed_comments_read on feed_comments
  for select to authenticated
  using (public.is_approved() or public.is_admin());

create policy feed_comments_insert_own on feed_comments
  for insert to authenticated
  with check (user_id = (select auth.uid()) and public.is_approved());

create policy feed_comments_delete on feed_comments
  for delete to authenticated
  using (user_id = (select auth.uid()) or public.is_admin());

-- ---------- Bucket na zdjęcia z bingo ----------
-- Prywatny jak proofs, ale z inną polityką odczytu: dowody przelewu widzi
-- wyłącznie admin, bo to dane finansowe; zdjęcia z bingo widzi każdy
-- zaakceptowany, bo na tym polega feed.
insert into storage.buckets (id, name, public, file_size_limit, allowed_mime_types)
values ('bingo', 'bingo', false, 5242880,
        array['image/jpeg', 'image/png', 'image/webp'])
on conflict (id) do nothing;

create policy bingo_insert_own on storage.objects
  for insert to authenticated
  with check (
    bucket_id = 'bingo'
    and (storage.foldername(name))[1] = (select auth.uid())::text
  );

create policy bingo_read_approved on storage.objects
  for select to authenticated
  using (bucket_id = 'bingo' and (public.is_approved() or public.is_admin()));

-- ---------- Ustawienia bonusów ----------
-- Strojone bez wdrożenia. Linii jest dwanaście: pięć wierszy, pięć kolumn,
-- dwie przekątne. Przy tych wartościach pełna plansza daje drużynie 1300 punktów.
insert into app_settings (key, value) values
  ('bingo_bonus_linia',   '50'),
  ('bingo_bonus_plansza', '200')
on conflict (key) do nothing;

-- ---------- Zasiew dwudziestu pięciu zadań ----------
-- PLACEHOLDERY. Dobre bingo stoi na wewnętrznych żartach i nazwiskach, więc te
-- zadania trzeba przed wyjazdem przejrzeć i podmienić w panelu albo zapytaniem.
-- Rejestr jest sekciarski celowo: komizm ma brać się z kontrastu formy z treścią.
insert into bingo_tasks (position, title, description) values
  (0,  'Zgromadzenie',            'Cała drużyna w jednym kadrze, przy wejściu do ośrodka'),
  (1,  'Ofiara ze snu',           'Wschód słońca widziany bez uprzedniego snu'),
  (2,  'Namaszczenie woźnicy',    'Zdjęcie z kierowcą autokaru'),
  (3,  'Sen niewłaściwy',         'Ktoś śpi w miejscu do tego nieprzeznaczonym'),
  (4,  'Formacja',                'Drużyna ustawiona w dowolną figurę geometryczną'),
  (5,  'Uczta',                   'Ktoś przygotowuje posiłek dla całej drużyny'),
  (6,  'Zdrada',                  'Zdjęcie z osobą z obcej drużyny, która na to przystała'),
  (7,  'Relikwia',                'Najbrzydszy przedmiot znaleziony w pokoju'),
  (8,  'Triumf',                  'Dowód wygranej w dowolnej grze'),
  (9,  'Nieuwaga',                'Cała drużyna w kadrze, nikt nie patrzy w obiektyw'),
  (10, 'Uczony',                  'Ktoś czyta książkę w trakcie imprezy'),
  (11, 'Zwierzę',                 'Zdjęcie z dowolnym miejscowym stworzeniem'),
  (12, 'Odtworzenie',             'Drużyna odgrywa scenę z filmu'),
  (13, 'Pokuta',                  'Ktoś biegnie o świcie'),
  (14, 'Połączenie zakazane',     'Najdziwniejsze zestawienie jedzenia na jednym talerzu'),
  (15, 'Porządek',                'Dowód sprzątniętego pokoju, datowany'),
  (16, 'Pieśń',                   'Ktoś śpiewa przy akompaniamencie czegokolwiek'),
  (17, 'Audiencja',               'Wspólne zdjęcie z organizatorem wyjazdu'),
  (18, 'Objawienie',              'Widok, który warto zapamiętać'),
  (19, 'Nauka bezużyteczna',      'Ktoś uczy kogoś czegoś zupełnie zbędnego'),
  (20, 'Komplet przed północą',   'Cała drużyna w jednym miejscu przed 24:00'),
  (21, 'Przełamanie',             'Dowód, że ktoś zjadł coś, czego się bał'),
  (22, 'Toast',                   'Najlepszy toast wieczoru, uwieczniony'),
  (23, 'Zaśnięcie w pół zdania',  'Dokładnie to, co w tytule'),
  (24, 'Ostatni kadr',            'Ostatnie zdjęcie wyjazdu');

-- Realtime celowo nieruszane. Ranking reaguje na żywo, bo subskrybuje
-- points_ledger, a bingo pisze do tej samej księgi — kryterium „punkty widać bez
-- odświeżania" jest więc spełnione bez ani jednej nowej subskrypcji. Dopisywanie
-- tabel do publikacji, których nic nie słucha, to martwa konfiguracja.
```

- [ ] **Step 3: Zastosuj w obu projektach**

```bash
npx supabase db push
npx supabase link --project-ref cmuyeoobmidawmyxwihk
npx supabase db push
npx supabase link --project-ref tjjlslupthlylnxvfroz
```

Jeśli `db push` odbije się od `must be owner of table objects`, polityki bucketu
załóż ręcznie w panelu (Storage → Policies), **nie usuwając ich z pliku**.

- [ ] **Step 4: Potwierdź**

```bash
npx supabase migration list
npx supabase db query --linked "select count(*) as zadan from bingo_tasks"
```

Expected: siedem migracji z wypełnioną kolumną `remote`, `zadan: 25`.

- [ ] **Step 5: Commit**

```bash
git add supabase
git commit -m "Dodaj migrację bingo i feedu"
```

---

## Task 2: Funkcja akceptacji z bonusami

Serce tego planu. Akceptacja, punkty, wykrycie linii i bonusy w jednej transakcji.

**Files:**
- Create: `supabase/migrations/<timestamp>_akceptacja_bingo.sql`

- [ ] **Step 1: Utwórz plik i wpisz treść**

```bash
npx supabase migration new akceptacja_bingo
```

Sprawdź znacznik czasu jak poprzednio. Treść:

```sql
-- ============================================================
-- Sekta Wyjazdowa — akceptacja zgłoszeń bingo
-- ============================================================

-- Wszystko w jednej funkcji i jednej transakcji: zmiana statusu, punkty za
-- zadanie, wykrycie linii i bonusy. Rozbicie tego na osobne wywołania pozwoliłoby
-- dwóm adminom klikającym równocześnie przyznać ten sam bonus dwa razy.
create function public.review_bingo(
  p_submission_id uuid,
  p_approve       boolean,
  p_note          text default null
)
returns void
language plpgsql
security definer
set search_path = ''
as $$
declare
  v_team     uuid;
  v_user     uuid;
  v_task     uuid;
  v_punkty   integer;
  v_tytul    text;
  v_status   public.user_status;
  v_zapalone integer[];
  v_bonus_l  integer;
  v_bonus_p  integer;
  v_linia    record;
begin
  -- SECURITY DEFINER omija RLS, więc uprawnienie sprawdzamy ręcznie.
  if not public.is_admin() then
    raise exception 'Tylko admin moze rozpatrywac zgloszenia bingo';
  end if;

  if p_approve is null then
    raise exception 'Brak decyzji: p_approve nie moze byc null';
  end if;

  -- Blokada wiersza razem z warunkiem na status: drugi admin klikający to samo
  -- zgłoszenie zobaczy już zmieniony status i wyjdzie z czytelnym błędem.
  select team_id, user_id, task_id
    into v_team, v_user, v_task
  from public.bingo_submissions
  where id = p_submission_id and status = 'pending'
  for update;

  if v_team is null then
    raise exception 'Zgloszenie nie istnieje albo zostalo juz rozpatrzone';
  end if;

  v_status := (case when p_approve then 'approved' else 'rejected' end)::public.user_status;

  update public.bingo_submissions
  set status      = v_status,
      reviewed_by = auth.uid(),
      reviewed_at = now(),
      review_note = p_note
  where id = p_submission_id;

  if not p_approve then
    return;
  end if;

  -- ---------- Punkty za zadanie ----------
  select points, title into v_punkty, v_tytul
  from public.bingo_tasks where id = v_task;

  insert into public.points_ledger
    (user_id, team_id, delta, category, reason, ref_type, ref_id, awarded_by)
  values
    (v_user, v_team, v_punkty, 'bingo', v_tytul,
     'bingo_zadanie', p_submission_id::text, auth.uid());

  -- ---------- Stan planszy ----------
  select coalesce(array_agg(t.position), array[]::integer[])
    into v_zapalone
  from public.bingo_submissions s
  join public.bingo_tasks t on t.id = s.task_id
  where s.team_id = v_team and s.status = 'approved';

  select (value #>> '{}')::integer into v_bonus_l
  from public.app_settings where key = 'bingo_bonus_linia';
  select (value #>> '{}')::integer into v_bonus_p
  from public.app_settings where key = 'bingo_bonus_plansza';

  -- ---------- Bonusy za linie ----------
  -- Dwanaście linii: pięć wierszy, pięć kolumn, dwie przekątne. Operator `<@`
  -- sprawdza, czy wszystkie pola linii są wśród zapalonych.
  --
  -- Idempotentność bierze się z danych, nie z ostrożności: przed dopisaniem
  -- sprawdzamy, czy wpis o tym ref_type i ref_id już dla tej drużyny istnieje.
  for v_linia in
    select * from (values
      ('wiersz-0',    array[ 0, 1, 2, 3, 4]),
      ('wiersz-1',    array[ 5, 6, 7, 8, 9]),
      ('wiersz-2',    array[10,11,12,13,14]),
      ('wiersz-3',    array[15,16,17,18,19]),
      ('wiersz-4',    array[20,21,22,23,24]),
      ('kolumna-0',   array[ 0, 5,10,15,20]),
      ('kolumna-1',   array[ 1, 6,11,16,21]),
      ('kolumna-2',   array[ 2, 7,12,17,22]),
      ('kolumna-3',   array[ 3, 8,13,18,23]),
      ('kolumna-4',   array[ 4, 9,14,19,24]),
      ('przekatna-a', array[ 0, 6,12,18,24]),
      ('przekatna-b', array[ 4, 8,12,16,20])
    ) as l(nazwa, pola)
  loop
    if v_linia.pola <@ v_zapalone then
      if not exists (
        select 1 from public.points_ledger
        where team_id = v_team
          and ref_type = 'bingo_linia'
          and ref_id = v_linia.nazwa
      ) then
        -- Bonus jest zasługą zbiorową, więc idzie jako wpis drużynowy
        -- (user_id NULL). Nie podnosi niczyjego salda indywidualnego.
        insert into public.points_ledger
          (user_id, team_id, delta, category, reason, ref_type, ref_id, awarded_by)
        values
          (null, v_team, v_bonus_l, 'bingo_bonus',
           'Linia ' || v_linia.nazwa, 'bingo_linia', v_linia.nazwa, auth.uid());
      end if;
    end if;
  end loop;

  -- ---------- Bonus za pełną planszę ----------
  if array_length(v_zapalone, 1) = 25 then
    if not exists (
      select 1 from public.points_ledger
      where team_id = v_team and ref_type = 'bingo_plansza'
    ) then
      insert into public.points_ledger
        (user_id, team_id, delta, category, reason, ref_type, ref_id, awarded_by)
      values
        (null, v_team, v_bonus_p, 'bingo_bonus',
         'Pelna plansza', 'bingo_plansza', 'plansza', auth.uid());
    end if;
  end if;
end;
$$;

revoke execute on function public.review_bingo(uuid, boolean, text) from anon, public;
grant  execute on function public.review_bingo(uuid, boolean, text) to authenticated;
```

- [ ] **Step 2: Zastosuj w obu projektach i potwierdź**

Ta sama sekwencja co w Tasku 1. Potem:

```bash
npx supabase db query --linked "select proname from pg_proc where proname = 'review_bingo'"
```

Expected: jeden wiersz.

- [ ] **Step 3: Commit**

```bash
git add supabase
git commit -m "Dodaj funkcję akceptacji zgłoszeń bingo"
```

---

## Task 3: Testy polityk bingo i feedu

**Files:**
- Create: `tests/db/bingo-rls.test.ts`

- [ ] **Step 1: Napisz testy**

Utwórz `tests/db/bingo-rls.test.ts`. Wzoruj się na `tests/db/registrations.test.ts`
— ten sam kształt, te same pomocniki z `tests/helpers/supabase.ts`
(`sprzatanieUzytkownikow`, `ustawJakoZaakceptowany`, `signIn`, `anonimowy`).

Testy do napisania:

1. **zaakceptowany widzi zadania** — odczyt `bingo_tasks` zwraca 25 wierszy
2. **oczekujący nie widzi zadań** — świeży użytkownik bez akceptacji dostaje pusty zbiór
3. **uczestnik składa zgłoszenie dla własnej drużyny** — insert przechodzi
4. **nie złoży zgłoszenia w cudzym imieniu** — `user_id` kogoś innego, błąd
5. **nie złoży zgłoszenia dla cudzej drużyny** — własny `user_id`, cudze `team_id`, błąd
6. **nie wskaże cudzego pliku** — `photo_path` w cudzym folderze, błąd
7. **nie złoży drugiego zgłoszenia do tego samego pola** — naruszenie unikalności
8. **odrzucenie zwalnia pole** — po ustawieniu statusu `rejected` kluczem serwisowym drugie zgłoszenie przechodzi
9. **autor wycofuje własne oczekujące** — delete zwraca skasowany wiersz
10. **nie wycofa cudzego** — delete zwraca pustą listę
11. **nie wycofa zaakceptowanego** — delete zwraca pustą listę, wiersz zostaje
12. **nikt nie zmieni statusu zwykłym UPDATE-em** — pusta lista zmienionych wierszy

**Uwaga do testu 8:** unikalność obejmuje wyłącznie nieodrzucone, więc to sprawdza
konkretnie ten warunek, a nie samą unikalność.

- [ ] **Step 2: Uruchom i commit**

```bash
npm test -- tests/db/bingo-rls.test.ts
```

Expected: `12 passed`

```bash
git add tests
git commit -m "Dodaj testy polityk bingo"
```

---

## Task 4: Testy akceptacji i bonusów

Najważniejszy zestaw w tym planie. Logika linii jest jedynym miejscem, gdzie błąd
byłby cichy: bonus nie przyszedłby albo przyszedł dwa razy, a nikt by nie zauważył
do końca wyjazdu.

**Files:**
- Create: `tests/db/bingo-akceptacja.test.ts`

- [ ] **Step 1: Napisz testy**

Potrzebny będzie pomocnik zapalający dowolny zestaw pól dla drużyny — kluczem
serwisowym, z pominięciem funkcji:

```ts
/** Zapala wskazane pola planszy dla drużyny, omijając akceptację. */
async function zapal(teamId: string, userId: string, pozycje: number[]) {
  const { data: zadania } = await admin
    .from("bingo_tasks")
    .select("id, position")
    .in("position", pozycje);

  for (const z of zadania ?? []) {
    await admin.from("bingo_submissions").insert({
      team_id: teamId,
      user_id: userId,
      task_id: z.id,
      photo_path: `${userId}/${crypto.randomUUID()}.jpg`,
      status: "approved",
    });
  }
}
```

Testy:

1. **uczestnik nie rozpatrzy zgłoszenia** — `rpc` zwraca błąd
2. **niezalogowany nie wywoła funkcji** — `anonimowy()`, komunikat **nie** wspomina
   o adminie (dowód, że `revoke ... from anon` działa, a nie strażnik w ciele)
3. **admin akceptuje: pole zapalone, punkty u autora** — status `approved`,
   w księdze wpis `bingo_zadanie` z `user_id` autora i `delta` równą `points` zadania
4. **admin odrzuca z notatką** — status `rejected`, notatka zapisana, **zero wpisów
   w księdze**
5. **zapalenie linii daje bonus raz** — zapal cztery pola wiersza, zgłoś piąte,
   zaakceptuj; w księdze jeden wpis `bingo_linia` o `ref_id = 'wiersz-0'`,
   z `user_id = null`
6. **bonus nie przychodzi drugi raz** — zapal kolejne pole w innej linii i zaakceptuj;
   liczba wpisów `ref_id = 'wiersz-0'` nadal wynosi jeden
7. **przekątna też liczy się jako linia** — zapal 0, 6, 12, 18 i zgłoś 24
8. **pełna plansza daje bonus planszy** — zapal 24 pola, zgłoś ostatnie; wpis
   `bingo_plansza` istnieje dokładnie raz
9. **rozpatrzone zgłoszenie nie da się rozpatrzyć drugi raz** — drugie wywołanie zwraca błąd
10. **brak decyzji jest odrzucany** — `p_approve: null`

**Sprzątanie:** `afterEach` musi kasować wiersze `points_ledger` o kategoriach
`bingo` i `bingo_bonus` **przed** kasowaniem użytkowników, oraz wszystkie
`bingo_submissions`. Inaczej bonusy zostaną w bazie i test 6 zacznie padać
przy kolejnym przebiegu, bo wpis `wiersz-0` dla tej drużyny już będzie istniał.

**To jest najczęstsza pułapka w tym pliku** — bonusy są przypisane do drużyny,
a drużyny są zasiane na stałe i współdzielone między testami.

- [ ] **Step 2: Uruchom i commit**

```bash
npm test -- tests/db/bingo-akceptacja.test.ts
```

Expected: `10 passed`

```bash
git add tests
git commit -m "Dodaj testy akceptacji bingo i bonusów za linie"
```

---

## Task 5: Testy warstwy społecznej

**Files:**
- Create: `tests/db/feed-spoleczne.test.ts`

- [ ] **Step 1: Napisz testy**

1. **zaakceptowany stawia lajk** — insert przechodzi
2. **drugi lajk tej samej osoby pada** — naruszenie klucza głównego
3. **lajk można cofnąć** — delete własnego zwraca wiersz
4. **nie cofnie cudzego lajka** — pusta lista
5. **oczekujący nie postawi lajka** — świeży użytkownik bez akceptacji, błąd
6. **zaakceptowany komentuje** — insert przechodzi
7. **pusty komentarz pada** — `body` z samych spacji
8. **za długi komentarz pada** — 501 znaków
9. **autor kasuje własny komentarz** — zwraca wiersz
10. **uczestnik nie skasuje cudzego** — pusta lista
11. **admin kasuje cudzy** — zwraca wiersz

- [ ] **Step 2: Uruchom cały pakiet i commit**

```bash
npm test
```

Expected: `98 passed` w dwunastu plikach (65 wcześniejszych + 12 + 10 + 11)

```bash
git add tests
git commit -m "Dodaj testy lajków i komentarzy"
```

---

## Task 6: Plansza i wrzucanie zdjęcia

**Files:**
- Modify: `src/types/db.ts`
- Modify: `src/app/(apka)/bingo/page.tsx`
- Create: `src/app/(apka)/bingo/Plansza.tsx`

- [ ] **Step 1: Dopisz typy**

W `src/types/db.ts`:

```ts
export type BingoTask = {
  id: string;
  position: number;
  title: string;
  description: string;
  points: number;
  active: boolean;
};

export type BingoSubmission = {
  id: string;
  team_id: string;
  user_id: string;
  task_id: string;
  photo_path: string;
  caption: string | null;
  status: UserStatus;
  reviewed_by: string | null;
  reviewed_at: string | null;
  review_note: string | null;
  created_at: string;
};
```

- [ ] **Step 2: Napisz stronę planszy**

`src/app/(apka)/bingo/page.tsx` — Server Component. Czyta profil (drużyna),
`bingo_tasks` posortowane po `position` i `bingo_submissions` własnej drużyny.
Przekazuje do klienta.

Osoba bez drużyny widzi komunikat zamiast planszy — to możliwy stan, bo
akceptacja przypisuje drużynę, ale admin mógł ją wyczyścić.

- [ ] **Step 3: Napisz planszę**

`src/app/(apka)/bingo/Plansza.tsx` — klient. Siatka 5×5, każde pole w jednym
z trzech stanów:

- **puste** — szkło, tytuł zadania małym drukiem
- **oczekujące** — obramowanie w kolorze krwi, bez wypełnienia
- **zapalone** — wypełnienie krwią, tytuł na biało

Dotknięcie pola otwiera arkusz z pełnym opisem zadania i akcją zależną od stanu:
wrzuć zdjęcie (puste), wycofaj zgłoszenie (oczekujące, jeśli jesteś autorem),
nic (zapalone albo cudze oczekujące).

Wrzucanie idzie tą samą drogą co dowód przelewu: `skompresuj` z `@/lib/obrazy`,
upload do bucketu `bingo` pod `${user.id}/${crypto.randomUUID()}.jpg`, potem
insert. **Rygiel `useRef` przed pierwszym `await`**, jak wszędzie indziej.

OCR-u tu nie ma — to zdjęcie z imprezy, nie dokument.

- [ ] **Step 4: Sprawdź i commit**

```bash
npx tsc --noEmit && npx eslint src tests && npm run build
git add src && git commit -m "Dodaj planszę bingo z wrzucaniem zdjęć"
```

---

## Task 7: Feed z lajkami i komentarzami

**Files:**
- Modify: `src/app/(apka)/feed/page.tsx`
- Create: `src/app/(apka)/feed/Wpis.tsx`

- [ ] **Step 1: Napisz stronę feedu**

Server Component. Czyta zaakceptowane zgłoszenia z zagnieżdżonymi relacjami.

**Uwaga, na tym już raz polegliśmy:** `bingo_submissions` ma dwa klucze obce do
`profiles` — `user_id` i `reviewed_by`. Samo `profiles(display_name)` skończy się
błędem `PGRST201`. Trzeba wskazać relację jawnie:

```ts
    .select(
      "id, photo_path, caption, created_at, " +
      "bingo_tasks(title, points), teams(name, color), " +
      "profiles!bingo_submissions_user_id_fkey(display_name)",
    )
```

**Sprawdź `error`**, nie tylko `data` — cicho połknięty błąd renderuje pusty feed
i wygląda jak brak treści.

Podpisane URL-e do zdjęć generuj równolegle przez `Promise.all`, jak w kolejce
zgłoszeń.

- [ ] **Step 2: Napisz wpis**

`src/app/(apka)/feed/Wpis.tsx` — klient. Zdjęcie, autor, drużyna, nazwa zadania,
liczba punktów. Pod spodem przycisk lajka z licznikiem i lista komentarzy
z polem do dopisania.

Lajk działa optymistycznie: stan zmienia się od razu, a przy błędzie wraca.
Przy sześćdziesięciu pięciu osobach i jednym lajku na osobę licznik jest mały,
więc pobieramy wszystkie lajki wpisu i liczymy po stronie klienta.

Komentarze: autor i admin widzą przycisk kasowania.

- [ ] **Step 3: Sprawdź i commit**

```bash
npx tsc --noEmit && npx eslint src tests && npm run build
git add src && git commit -m "Dodaj feed z lajkami i komentarzami"
```

---

## Task 8: Kolejka bingo w panelu admina

**Files:**
- Create: `src/app/(apka)/admin/bingo/page.tsx`, `src/app/(apka)/admin/bingo/DecyzjaBingo.tsx`
- Modify: `src/app/(apka)/admin/page.tsx`

- [ ] **Step 1: Napisz kolejkę**

Wzoruj się na `src/app/(apka)/admin/rejestracje/page.tsx` — ten sam kształt:
lista oczekujących, zdjęcie przez podpisany URL, `loading="lazy"`, przyciski
decyzji. Zamiast danych zgłoszenia pokazujesz nazwę zadania, jego opis, autora
i drużynę.

`DecyzjaBingo.tsx` wywołuje `rpc("review_bingo", { p_submission_id, p_approve, p_note })`.
Rygiel `useRef`, surowe komunikaty błędów — admin zna projekt, a treść
`raise exception` jest dla niego informacją.

- [ ] **Step 2: Dopisz wejście w rozdrożu panelu**

W `src/app/(apka)/admin/page.tsx` dodaj czwarte wejście: `/admin/bingo`,
nazwa „Bingo", opis „Kolejka zdjęć z planszy".

- [ ] **Step 3: Sprawdź i commit**

```bash
npx tsc --noEmit && npx eslint src tests && npm run build && npm test
git add src && git commit -m "Dodaj kolejkę bingo w panelu admina"
```

---

## Task 9: Weryfikacja i wdrożenie

- [ ] **Step 1: Pełne sprawdzenie**

```bash
npx tsc --noEmit && npx eslint src tests && npm run build && npm test
```

Expected: `98 passed` w dwunastu plikach, nowe trasy `/admin/bingo` w tabeli.

- [ ] **Step 2: Nadaj rolę admina drugiej osobie**

Decyzja D2 ze speca: kolejkę obsługuje kilku adminów, nie jeden Kapłan. Rola
istnieje od migracji 0001 i panel działa dla każdego, kto ją ma — więc to jedno
zapytanie na osobę, zero kodu.

```sql
update profiles
set role = 'admin', status = 'approved'
where email in ('ktos@samorzad.ue.wroc.pl', 'ktos.inny@samorzad.ue.wroc.pl');
```

**Oba pola naraz.** Bramka sprawdza status przed rolą, więc admin ze statusem
innym niż `approved` odbije się na `/rejestracja` i nie wejdzie do panelu.

- [ ] **Step 3: Przejdź ścieżkę z telefonu**

Po wdrożeniu, na dwóch kontach (twoim i testowym):

1. Plansza pokazuje 25 pól, wszystkie puste.
2. Wrzucenie zdjęcia zmienia pole na oczekujące — **u całej drużyny**, nie tylko
   u autora.
3. Drugie zgłoszenie do tego samego pola nie przechodzi.
4. Wycofanie własnego zgłoszenia zwalnia pole.
5. Akceptacja zapala pole i dopisuje punkty; ranking rośnie **bez odświeżania**.
6. Zdjęcie pojawia się w feedzie.
7. Lajk działa i cofa się; drugi raz się nie da.
8. Komentarz można dodać i skasować własny.
9. Odrzucenie z notatką zwalnia pole.

- [ ] **Step 4: Sprawdź bonus za linię na żywo**

To jedyna rzecz, której nie sprawdzisz klikając przypadkiem. W SQL Editorze zapal
cztery pola wiersza dla swojej drużyny, potem wrzuć i zaakceptuj piąte przez apkę.
Ranking ma skoczyć o punkty zadania **plus** bonus za linię, a w historii księgi
mają być dwa wpisy: `bingo` i `bingo_bonus`.

- [ ] **Step 5: Wdróż**

Push na `main` uruchamia produkcję automatycznie.

```bash
git push origin main
```

---

## Stan po tym planie

Ranking przestaje pokazywać zera. Dwie puste zakładki wypełniają się treścią.
Aplikacja ma pierwsze źródło punktów niezależne od ręcznej pracy Kapłana
i pierwszą warstwę społeczną.

Nie działa jeszcze: sklepik (plan 05), kasyno i mini-gra (06), gossipy (07),
powiadomienia (08), harmonogram i ekrany użytkowe — te ostatnie nie mają
jeszcze specu.

**Warunek na wyjazd:** dwadzieścia pięć zadań to placeholdery. Przed wyjazdem
trzeba je podmienić na wasze — dobre bingo stoi na wewnętrznych żartach,
nazwiskach i tym, co faktycznie się wydarzy. Punktacja też jest startowa:
pełna plansza daje dziś 1300 punktów, co przy czterech drużynach uczyni bingo
dominującym źródłem w rankingu. Obie rzeczy zmienia się bez wdrażania kodu.
